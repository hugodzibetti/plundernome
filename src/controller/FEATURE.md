# Feature: Controller Layer

## Owned files: src/controller/*
## Depends on: services layer interfaces, domain types
## Dependents: UI views
## Agent notes: Mediates UI ↔ services. Orchestrates user actions (search, download, install, launch). No direct GI imports — uses service interfaces.
