# Feature: UI Widgets

## Owned files: src/ui/widgets/*
## Depends on: domain types (models)
## Dependents: UI views
## Agent notes: Reusable GTK4 widgets: game-card, progress-bar, compat-badge, download-row, source-config. Pure presentation — no business logic. Accept data via constructors/setters, emit GTK signals.
